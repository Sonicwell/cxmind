/**
 * Shared widget type definitions.
 *
 * Re-exports from `dashboard/types.ts` and extends for Agent Map cards.
 * Both Dashboard and Agent Map registries should reference these types.
 */
export type { WidgetDef, WidgetProps, WidgetCategory, UserRole } from '../dashboard/types';
